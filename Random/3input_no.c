#include <stdio.h>

int main() {
    int a;
    int b;
    printf("Enter the value for a - ", a);
    scanf("%d", &a);
    printf("Enter the value for b - ", b);
    scanf("%d", &b);

    printf("Sum of a and b is %d", a+b);

    return 0;
}